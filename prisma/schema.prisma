generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlserver"
  url      = env("DATABASE_URL")
}

model UserInfo {
  id        String @id
  firstName String
  lastName  String
  email     String
  password  String
}

model Achievements {
  id     String  @id
  achv_1 Boolean
  achv_2 Boolean
  achv_3 Boolean
}

model Activities {
  id       String @id
  topic    String
  activity String
  desc     String
}
